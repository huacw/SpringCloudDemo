##优先级低于application.yml(.properties),通常用来设置默认值
#server:
#  port: 7762
#
spring:
  profiles:
    active: pee1
#
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    registerWithEureka: false
#    fetchRegistry: false
#    serviceUrl:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/